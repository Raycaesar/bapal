# BAPAL 验证说明

## `^φ` 的预期语义

本项目把输入语法 `^φ` 实现为 BAPAL 的存在性布尔任意公告算子。其语义意图是：

在当前世界 `w`，`^φ` 为真，当且仅当存在一个布尔/命题公告 `α`，使得：

1. `α` 在当前世界 `w` 为真；
2. 公开公告 `α` 后，模型被限制到所有满足 `α` 的世界；
3. 在限制后的模型中，`φ` 在 `w` 为真。

这里的布尔公告只允许使用命题原子和布尔连接词，例如否定、合取、析取、蕴含、等价。它不允许包含知识算子、公开公告算子或 BAPAL 算子。

## 为什么有限模型中枚举 valuation classes 是正确的

在有限模型中，每个世界都有一个命题赋值，也就是每个命题原子在该世界为真或为假。两个世界如果对所有命题原子的真假赋值完全相同，它们属于同一个 propositional valuation class。

布尔公式的真值只依赖命题赋值。因此，一个布尔公告 `α` 在模型中选出的世界集合一定是若干 valuation classes 的并集。反过来，在有限模型中，任意若干 valuation classes 的并集都可以由某个布尔公式描述：对每个目标 valuation class 写出对应的命题合取式，再把这些合取式析取起来即可。

所以，在有限模型里，对所有布尔公告量化，可以等价地实现为：枚举所有 propositional valuation classes 的并集，并且只考虑包含当前世界 valuation class 的并集。后一条件对应“公告 `α` 在当前世界为真”。

## 为什么相同命题赋值的世界不能被分开

如果两个世界拥有完全相同的命题赋值，那么任何只由命题原子和布尔连接词构成的公式，在这两个世界上的真值都相同。

因此，不存在一个布尔公告 `α` 能让 `α` 在其中一个世界为真、在另一个世界为假。BAPAL 的布尔公告可以删除或保留整个 valuation class，但不能在同一个 valuation class 内部分离世界。

## 测试覆盖内容

`scripts/check-logic-regressions.js` 覆盖以下回归点：

1. `^φ` 的解析和 pretty-printing：检查 `^K{a}p` 的 ASCII、JSON、Unicode 和 LaTeX 输出。
2. BAPAL 能区分不同命题赋值的世界：在包含 `p` 世界和非 `p` 世界的模型中，`^K{a}p` 可以通过布尔公告 `p` 成立。
3. BAPAL 不能区分相同命题赋值的世界：即使对象属性顺序不同，`{p,q}` 和 `{q,p}` 仍被视为同一个 valuation class。
4. 公开公告 `[A]B` 仍然工作：检查 `[p]K{a}p` 在保留世界和被删除世界上的标准 PAL 行为。
5. S5 closure 会为 agent 创建自反、对称、传递的关系。
6. UI 中隐藏的自环仍存在于模型里，并被知识语义使用：自环存在时，`K{a}~p` 在 `p` 世界为假。

## 仍未证明的内容

这些测试是实现级回归测试，不是数学完备性证明。它们不能证明：

1. BAPAL 语义对所有有限模型和所有公式都完全正确；
2. parser 对所有复杂嵌套公式都无歧义；
3. S5 编辑模式在所有 UI 操作序列下都不会产生异常状态；
4. 当前实现相对于 BAPAL 的公理系统是可靠且完备的。

它们的作用是覆盖本项目最关键的语义约定和最近改动的高风险路径，帮助防止解析、valuation class 枚举、PAL 求值和 S5 闭包行为发生回归。
